{
  "name": "submit_passage",
  "description": "Submit the generated reading passage and its title. Always call this tool with the complete passage — never return passage text as plain text.",
  "input_schema": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "Engaging title for the passage, maximum 8 words."
      },
      "passage": {
        "type": "string",
        "description": "Full passage text, strictly 220-260 words, NCERT-aligned, Indian context."
      }
    },
    "required": ["title", "passage"]
  }
}
